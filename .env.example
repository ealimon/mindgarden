# Only needed once the Memory Lane / Family Voices modules are wired to a
# real backend (Firebase, per the hosting discussion). Not required for the
# 5 modules that already work locally.
# VITE_FIREBASE_API_KEY=
# VITE_FIREBASE_PROJECT_ID=
# VITE_FIREBASE_STORAGE_BUCKET=
